import { StringMap } from "@angular/compiler/src/compiler_facade_interface";

export class Course{
    id: number;
    name: string;
    imageUrl: string;
    price: number;
    code: String;
    duration: number;
    rating: number;
    releaseDate: string
    description: string
}